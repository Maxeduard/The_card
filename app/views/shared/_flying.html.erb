<div class="fix">
  <div class="flying">
    <%= link_to order_path(current_user.current_order(session[:restaurant_id])) do%>
      <h1 class="order">🛒</h1>
    <% end %>
    <div class="try">
      <%= link_to restaurant_path(session[:restaurant_id]) do%>
      <h1 class="add">+</h1>
      <% end %>
    </div>

    <%= link_to orders_path do%>
    <h1 class="pay"><i class="fas fa-receipt"></i></h1>
    <% end %>
  </div>
</div>
